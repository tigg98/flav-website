/**
 * Site-wide feature flags.
 *
 * SHOW_FLAV_PLUS — gates the Flav+ consumer pricing (Free / Flav+ / Lifetime)
 * on the homepage and the /pricing route. Flip to `true` on v1.1 launch day.
 */
export const SHOW_FLAV_PLUS = false;
